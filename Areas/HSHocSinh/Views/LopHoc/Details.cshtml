@model App.Areas.HSHocSinh.Models.LopHoc

@{
    ViewData["Title"] = "Chi Tiết Lớp Học";
}

<partial name="_StatusMessage" />
<div>
    <h4>LopHoc</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.name)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.ChuNghiem)
        </dt>
        <dd class = "col-sm-10">
            <a class="col-sm-5" asp-controller="ChuNghiem" asp-action="Details" asp-route-id="@Model.ChuNghiem?.Id">
                @Html.DisplayFor(model => model.ChuNghiem.name)
            </a>
        </dd>
        <dt class = "col-sm-2">Học Sinh</dt>
        <table class="table fixed-size">
            <thead>
                <tr>
                <th scope="col">name</th>
                <th scope="col">SDT</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var HocSinhs in Model.hocSinhs) {
                    <tr>
                        <td>
                            <a class="col-sm-5" asp-controller="HocSinh" asp-action="Details" asp-route-id="@HocSinhs.Id">@HocSinhs.name</a>
                        </td>
                        <td>@HocSinhs.SDT</td>
                    </tr>
                }
            </tbody>
        </table>
    </dl>
</div>
<div>
    <a class="btn btn-sm btn-success" asp-action="Edit" asp-route-id="@Model?.Id">Edit</a> |
    <a class="btn btn-sm btn-info" asp-action="Index">Back to List</a>
</div>

@{
    _AdminSidebarService.SetActive("LopHoc", "Index", "HSHocSinh");
}
@section Sidebar
{
    @Html.Raw(_AdminSidebarService.RenderHtml())
}